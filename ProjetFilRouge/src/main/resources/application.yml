spring:
 datasource:
   url: jdbc:h2:file:~/filrouge
   
 jpa:    
  hibernate:
   ddl-auto: create
# spring data jpa
 # jpa:
    #hibernate:
     # ddl-auto: create ou update
      
 #   show-sql: true

#spring:
 # datasource:
  #  url: jdbc:postgresql://localhost:5432/Gestion
   # username: postgres
    #password: 

  #jpa:
     
      
server:
  servlet:
    context-path: /projetfilrouge